services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: rag_core
      POSTGRES_USER: rag_core
      POSTGRES_PASSWORD: rag_core
    ports:
      - "5432:5432"
    volumes:
      - rag_core_pgdata:/var/lib/postgresql/data

volumes:
  rag_core_pgdata:
